#include "main.h"

/**
 * mul - multiplies two integers
 * @a: holds the first value
 * @b: holds the second vaulue
 *
 * Return: returns the product of the numbers
 */

int mul(int a, int b)
{
	int c = a * b;

	return (c);
}
